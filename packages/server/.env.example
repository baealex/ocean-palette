# Ocean Palette server environment example
# Copy this file to packages/server/.env for source-based local runs.
# Docker images already set this default in packages/server/Dockerfile.

# SQLite database URL used by Prisma and the server runtime.
# Keep this path aligned with the deployment volume strategy.
DATABASE_URL="file:./prisma/data/db.sqlite3"
